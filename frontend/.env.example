# Base URL of the YARP gateway. Every API call goes through it.
VITE_API_BASE_URL=http://localhost:5080/api

# Only needed once the MSAL / Entra ID sign-in replaces the seeded demo accounts.
VITE_AZURE_AD_CLIENT_ID=your-client-id
VITE_AZURE_AD_TENANT_ID=your-tenant-id
