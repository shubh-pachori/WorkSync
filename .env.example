# Copy to .env and change the password before starting anything.
# .env is git-ignored; docker-compose reads it, and scripts/set-dev-secrets.sh
# reuses POSTGRES_PASSWORD so the services and the database agree.

POSTGRES_USER=aitimesheet
POSTGRES_PASSWORD=change-me-before-running
POSTGRES_DB=postgres
POSTGRES_PORT=5432
